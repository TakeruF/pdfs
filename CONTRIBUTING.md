# Contributing

PDF Organizerへの不具合報告、改善提案、Pull Requestを歓迎します。

## Issue

- 不具合の場合は、ブラウザとOS、再現手順、期待する結果、実際の結果を記載してください。
- PDFに個人情報や機密情報が含まれる場合、そのファイルをIssueへ添付しないでください。可能なら問題を再現する最小限のサンプルを新しく作成してください。
- 新機能の場合は、解決したい問題と想定する操作方法を記載してください。

## Pull Request

1. リポジトリをforkし、変更内容を表すブランチを作成します。
2. `npm ci` で依存関係をインストールします。
3. 変更を実装し、`npm run build` と `npm audit` を実行します。
4. UI変更では、主要ブラウザでPDFの読み込みと書き出しを確認します。
5. Pull Requestに変更理由、動作への影響、確認方法を記載します。

変更は小さく保ち、PDFや認証情報などの機密データをコミットしないでください。
